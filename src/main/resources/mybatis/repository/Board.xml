<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-///mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="whitekim.mybatisblog.repository.BoardRepository">
    <resultMap id="Board" type="whitekim.mybatisblog.model.Board" autoMapping="false">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="at_created_time" property="atCreatedTime"/>
        <result column="at_updated_time" property="atUpdatedTime"/>
        <result column="is_deleted" property="isDeleted"/>
        <collection property="replyList" resultMap="Reply" javaType="java.util.ArrayList" ofType="whitekim.mybatisblog.model.Reply"/>
    </resultMap>
    <resultMap id="Member" type="whitekim.mybatisblog.model.Member">
        <id column="id" property="id"/>
        <result column="username" property="title"/>
        <result column="password" property="content"/>
        <result column="nickname" property="at_created_time"/>
        <result column="at_created_time" property="atCreatedTime"/>
        <result column="at_updated_time" property="atUpdatedTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>
    <resultMap id="Reply" type="whitekim.mybatisblog.model.Reply">
        <id column="reply_id" property="id"/>
        <result column="message" property="message"/>
        <result column="at_created_time" property="atCreatedTime"/>
        <result column="at_updated_time" property="atUpdatedTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>
    <select id="findById" parameterType="Long" resultMap="Board">
        select
            b.id,
            b.title,
            b.content,
            b.author,
            b.at_created_time,
            b.at_updated_time,
            b.is_deleted,
            r.id,
            r.board,
            r.message,
            r.at_created_time,
            r.at_updated_time
        from Board b join Reply r on r.board = b.id
        where b.is_deleted = false and b.id = #{id};
    </select>
    <select id="findAll" resultType="whitekim.mybatisblog.model.Board" parameterType="Long">
        select
            b.id board_id,
            b.title,
            b.content,
            b.author,
            b.at_created_time,
            b.at_updated_time,
            b.is_deleted
        from Board b left join Reply r on board_id = r.board
        where b.is_deleted = false
    </select>
    <insert id="save" parameterType="whitekim.mybatisblog.model.Board">
        insert into Board(title, content, author, at_created_time, at_updated_time)
        values (#{title}, #{content}, #{author.id}, now(), now())
    </insert>
    <update id="update" parameterType="whitekim.mybatisblog.model.Board">
        update board b
        set b.title = #{title}, b.content = #{content}, at_updated_time = now()
        where b.id = #{id}
    </update>
    <delete id="deleteById" parameterType="Long">
        delete from Board b where b.id = #{id}
    </delete>
</mapper>